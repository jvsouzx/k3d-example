apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: vaultwarden-pg
  namespace: vaultwarden
spec:
  instances: 2
  storage:
    size: 1Gi
  bootstrap:
    initdb:
      database: vaultwarden
      owner: vaultwarden
